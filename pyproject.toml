[project]
name = "job-watch"
version = "0.1.0"
description = "Two job watches: hiring.cafe's wide net, and named employers' own boards"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.13.10",
    "python-dotenv>=1.2.2",
]
