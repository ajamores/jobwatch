.env
.venv/
__pycache__/
watch.log

# state and generated output — every run rebuilds it, and seen*.json is per-machine
data/

# spreadsheets, meant for a human rather than for git
exports/

# personal: CV, salary target — the template is config/profile.example.md
config/profile.md
